# これは「AIっぽさ」を抜くデザイン案のデモです

`takumin-2525/editplus-kyoto-mock` のフォークです。**中身の判断材料としてだけ見てください。**
実装そのものは WordPress 側のブランチ `feat/de-ai-pass`（`takumin-2525/editplus-web-renewal`）にあります。

きっかけは「サイト全体がAIっぽく見える」という指摘です。何がそう見せているのかを
測ったうえで直した結果が、この見た目です。

---

## 1. 見た目：何を変えたか

| | 変更前 | 変更後 |
|---|---|---|
| 見出しの上の欧文ラベル（アイブロウ） | サイト全体で **26箇所**、トップだけで10回 | **全廃** |
| `letter-spacing` | **21種類・98箇所** | **5段のトークン**に固定 |
| 欄見出し | `clamp(23px→32px)` / weight 600 | `clamp(28px→46px)` / weight 500 |
| 写真が無いスポット | 3:2 の灰色の板に「NO IMAGE」が**7枚**（実写真5枚より多い） | 写真の枠自体を出さない |
| 日本語面のUI文言 | View all → / Read the story / Search / Map / Tel | すべて見る／記事を読む／検索／地図／電話 |
| 診断の進捗表示 | 金 `#9a8348`（`--panel` 上で **3.45:1**、AA未達） | `--gold-ink` へ |
| ヒーローの検索欄 | `outline:none` のまま（キーボードで辿ると**ここだけ**現在地が見えない） | `:focus-within` を追加 |

**アイブロウについて。** 2026-09-15 に「英字の小さな大文字は装飾としては綺麗だが、初見の旅行者には
情報にならない」（野口さん指摘）という判断がありましたが、適用されたのはヒーローだけで、
欄見出しの上の26箇所はそのまま残っていました。同じ判断を全体に通した形です。

**字間について。** 2026-09-16 のデザイン監査（`60_デザイン/`）に「字間は3〜5段階に固定して
トークン化」と自分たちで書かれていたので、それに合わせました。

**変えていないもの：カードの枠**（2026-09-16「枠は引く」＝先方判断）と**配色**（2026-07-13 先方選択）。
どちらも決定事項なので触っていません。

---

## 2. ⚠️ 写真を4点差し替えています（ライセンスの問題）

元の mock に入っていた写真のうち **4点に、撮影者名とCCライセンス表記が画像に焼き込まれていました。**
全43点を目視で確認した結果です。

| 差し替えた画像 | 焼き込まれていた文字 |
|---|---|
| `assets/images/qr-bg.jpg` | 左上 `cc-nc-nd` ／ 左下 `Trevor Dobson` |
| `uploads/2026/07/img01.*` | 左上 `cc-nc-` ／ 左下 `Patrick Vierthaler` |
| `uploads/2026/07/img02.*` | 左上 `cc-nc-sa` ／ 左下 `cygenta` |
| `uploads/2026/07/img09.*` | 左上 `cc-nc-nd` ／ 左下 `jpellgen (@1105_jp)` |

**CC の NC（非営利）は商用利用を禁じています。** この案件は広告主を取る前提なので商用にあたります。
ND（改変禁止）が付いているものは、トリミングや色調補正をして使うのも別途アウトです。
さらに、同じ画面に「編集部が実際に歩いて選んだ」「現地取材による一次情報」と書いてあるので、
その"証拠"として他人のCC写真が出ている状態でした。

**このフォークは public リポジトリなので、そのまま置いておくと再発行にあたります。**
なので、差し替えてから push しました。差し替え先は**誌面 vol.23 のスキャン（自社コンテンツ）**です：

- `img01` ← `vol23-p184`（町家の外観）
- `img02` ← `vol23-p178`（品物の棚）
- `img09` ← `vol23-p204`（抹茶と菓子）
- `qr-bg` ← `vol23-p189-spot269`（カウンターと職人）

被写体が元と一対一で対応しているわけではありません。**デザインの見え方を判断するための仮置き**です。

> 補足：`staticrypt` が暗号化するのは HTML だけで、**画像とCSSは平文のまま配信されます。**
> 元の mock では、パスワードなしで
> `…/wp-content/uploads/2026/07/img09.jpg` が 200 で取得できる状態でした（実測）。
> プレビューのパスワードは写真の保護にはなっていません。

**きれいだった39点**：`hero.jpg` ／ `01_看板` 5点 ／ **`vol23-p*` 27点** ／ デモバナー3点 ／ site-icon。
誌面由来の写真は全部クリーンです。問題は「つなぎ」の汎用写真4点だけに閉じています。

---

## 3. ⚠️ `style.css` の末尾は「モック専用」です

`PREVIEW ONLY` とコメントで囲んだブロックがあります。

mock は staticrypt で HTML を暗号化しているためマークアップを直せません。そこで、本来は
テンプレート側でやる変更（アイブロウの削除・UI文言の日本語化）を、**見た目だけ CSS で
再現しています**（`display:none` と `::after` の `content`）。

**WordPress のテーマにコピーしないでください。** テーマ側は `feat/de-ai-pass` で
テンプレートを正しく直してあります（i18n も、既にあった「地図」「電話」「検索」の
日本語キーを呼ぶ形に直しています）。

---

## 4. 第二回（2026-09-21）で足したもの

第一回でアイブロウを外したあとも既視感が残っていたので、装飾ではなく**構造**を見ました。

- **欄の節奏**：9つの section のうち7つが「同じ幅・同じ `padding:46px`・見出し左＋リンク右＋
  グリッド」で完全に同じ形でした。**誌面の欄だけを崩しています** — 表紙を大きく（clamp 260→420px）、
  欄幅を1380pxに広げ、上下を clamp(76→132px) に。影も他の欄では使っていないので、ここだけ「物」に見えます。
  崩すのは1箇所だけです（あちこち崩すと、ただの散らかりになります）。
  紙の本は、テンプレートにも参考にした Aman にも無い、この媒体だけが持っているものなので選びました。
- **字間**：残り83箇所をトークンへ。19種類 → 5段（ロゴの `.brand` は字間そのものが図案なので除外）。
- **字級**：11〜16px に0.5px刻みで10種類あったので、40箇所を整数に寄せました。
- **診断の文言**：`quiz.js` 直書きの `QUESTION n / N`・`CONCIERGE`・`SORRY`・`Your Route`・
  `Editors’ Pick` を廃止し、既存の `epQuizI18n`（`wp_localize_script`）経路へ。
  「質問」「コンシェルジュの提案」「編集部のおすすめ」を5言語ぶん `i18n.php` に追加しました。
- **見出しの階層**：カード見出しが h2 の下でいきなり h4（h3 が飛ぶ）だったので、7箇所を h3 に。
- **タップ領域**：44px を下回っていた ≡(32×32)・言語(21×25)・チップ(38.1px)・引き出しの欄リンク(43px)。

> ⚠️ 訂正：第一回の時点で「`.pdate` の欧文セリフはアイブロウの取り残し」と書きましたが**誤りでした**。
> `.ts-no` `.step-no` `.book-no` `.pagination` と合わせて「数字は欧文セリフ」という一貫した体系に
> なっています。触っていません。

## 5. まだ直していないこと

**コード側で直せるもの**

- **トップ「おすすめの場所」の出し方**（いちばん効くのに未着手）。6枚のうち3枚が同じとんかつ
  チェーンの別店舗（三条河原町店／四条河原町店／四条烏丸店）でした。`editplus_top_spots_query()` が
  既定で新着順なので、最後に入れたものがそのまま出ています。spot は309件あり、写真付きで
  「編集部が歩いた」感のあるもの（竹林の道・大覚寺・實光刃物など）は後ろのページに埋まっています。
  上の「編集部が、通う店」は `_ep_pick` で絞れているので、同じ絞り方を入れれば直ります。
  **こちらでは手を付けていません** — PHPを実行できる環境が無く、編集の出力を変える変更を
  動かさずに書きたくなかったためです。
- ヒーロー画像が 1200×1800 で、1280×752 に拡大表示（Retina で約2.13倍）。2560px 相当が要ります。

**コードでは直せないもの（WP管理画面・素材側）**

- `.tcard` の写真2枚が `alt=""`。装飾ではなく内容の写真なので、メディアライブラリ側で入れてください。
- 写真そのもの（上記2章）。

---

検証は静的なミラー（staging のHTMLに同じ変更を当てて描画）で行っています。
**実WPでの確認は未実施**です。
